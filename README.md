# sys-audit

Hostname, isletim sistemi, RAM, disk ve ag bilgilerini toplar.

Python 3.10+, `pip install -r requirements.txt`

```
python -m sys_audit
python -m sys_audit --json
python -m sys_audit -o rapor.json
```

Alaaddin Turgay
